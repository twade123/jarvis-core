# Scheduled Repair Run - 2026-04-01 16:04

## Type: correction

## Summary
Scheduled repair: processed 11 items (2 fixed, 9 skipped). Rebuilt corrupted connection_doctor.db.

## Context
### Repair Queue Results:
- **Fixed (2)**: agents.db and journeys.db - both passed integrity checks, WAL checkpoints clean. Transient degradation resolved.
- **Skipped - corrupted backups (5)**: connection_doctor_corrupted_*.db files are known backup copies from prior recovery efforts, not active databases.
- **Skipped - on-demand servers (3)**: mlx_cto (port 11501), ollama (port 11434), mlx_cso (port 11502) are designed to start on demand and stop when idle. Being down is expected behavior.
- **Skipped - needs approval (1)**: CLAUDE.md has 8 stale file references. Requires Tim's approval to modify.

### Additional Fix:
- connection_doctor.db itself was corrupted (stale journal file on mounted filesystem). Rebuilt from schema using init_connection_doctor_db(). Old data lost but schema intact.
- Mounted filesystem (Cowork sandbox) has sqlite write limitations - writes via copy-to-/tmp pattern work but direct writes fail with disk I/O error.

### Health Snapshot:
- Overall: healthy
- DBs: 100/106 healthy, 6 degraded
- APIs: 7/15 down (all on-demand local servers, expected)
- Open incidents: 0

## Tags
connection-doctor, scheduled-repair, db-rebuild
